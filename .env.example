VITE_OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_KEY

# Nota: copia este archivo a `.env` y reemplaza la clave.
# Nunca subas `.env` al control de versiones. Usa variables de entorno en tu plataforma de deploy (Vercel, Netlify, etc.).